# Day 01 - Personal Introduction Page

A simple HTML page introducing myself.

## What I learned:
- Basic HTML structure (`<!DOCTYPE>`, `<html>`, `<head>`, `<body>`)
- Headings (`<h1>`, `<h2>`, `<h3>`)
- Paragraphs (`<p>`)
- Unordered lists (`<ul>` + `<li>`)
- Images (`<img>`)

## How to view:
Open `index.html` in any browser.

## Live on GitHub:
[Front_End-practice](https://github.com/sehfz/Front_End-practice)